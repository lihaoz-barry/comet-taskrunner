# Comet Task Runner Configuration
# Copy this file to .env and set your own values

# Security: API Key for remote access (Required for LAN access)
COMET_API_KEY=change-me-to-a-secure-password

# Network Mode: Set to 'true' to restrict to localhost only (default: false for LAN access)
# When false (default): Server binds to 0.0.0.0 and is accessible from LAN
# When true: Server binds to 127.0.0.1 and is only accessible from localhost
# COMET_LOCAL_ONLY=false

# Optional: Comet Browser Path (if not in standard location)
# COMET_PATH=C:\Custom\Path\to\comet.exe
